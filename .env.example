PORT=5000
NODE_ENV=development
# Use a long random value in production; never use the development fallback.
JWT_SECRET=replace_with_a_long_random_secret
# Use a managed PostgreSQL URL in production. SQLite is development-only.
DATABASE_URL=postgresql://user:password@host:5432/dbname
CLIENT_URL=http://localhost:5173
